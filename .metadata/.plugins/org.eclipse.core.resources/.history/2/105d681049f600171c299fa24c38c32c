package TreeGenerator;

import java.util.LinkedList;
import java.util.Queue;

public class TreeNodeGenerator {
	public static TreeNode treeGenRange(String[] array)
	{
		TreeNode<String> root;
		if (array.length == 0) {
			return null;
		}
		root = new TreeNode<String>(array[0]);
		Queue<TreeNode<String>> que = new LinkedList<TreeNode<String>>();
		que.add(root);
		while ()
		return root;
	}
}
