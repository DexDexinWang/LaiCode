import java.util.Arrays;

public class Solution4 {

	public static void main(String[] args) {
		int[] input = {1,2,3,4,5,6, 9, 10,20,21};
		System.out.println(minDifference(input));
	}
	
	/**
	 * base case: 
	 * dp[0] = 0;
	 */
	static int min = 0;
	public static int minDifference(int[] array) {
		if (array == null || array.length == 0) {
			return 0;
		}
	}
	
	public static void minDifference(int[] array, int i, int[] sum) {
		if (i > array.length - 1) {
			min = Math.min(sum[0] + array[i], sum[1] + array[i]); 
		} else if (i == array.length - 1) {
			
		}
	}
	
	private static int differ(int i , int j) {
		
	}
	
}
